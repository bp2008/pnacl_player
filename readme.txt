To build this project, install the Google Native Client SDK and copy the project folder into "nacl_sdk/pepper_xx/examples/api/" where pepper_xx refers to the most recent stable version of the SDK.

Then you may build by running "make" in this project folder or by using the BUILD menu in Visual Studio.